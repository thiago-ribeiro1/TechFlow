server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/techflow
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true